# Components

import {
  allChartsList
} from "../../utils/all-components-list";
import { pascalCase } from "@vtex/shoreline";

<ComponentSummaryGrid mt="var(--sl-space-4)">
  {allChartsList.map((component) => {
    let href = `/charts/${component}`;
    return <ComponentSummary key={component} name={`chart-${component}`} href={href} />;
  })}
</ComponentSummaryGrid>
