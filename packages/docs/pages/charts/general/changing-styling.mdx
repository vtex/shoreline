# Changing styling
Most visual aspects can be changes in the `option` prop, See <a href="https://echarts.apache.org/handbook/en/concepts/style/">Echarts docs</a>

Override the default theme by modifying the `theme` prop.
<Preview name="chart-different-theme"/>
