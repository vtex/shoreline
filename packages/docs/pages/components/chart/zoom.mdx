# Zoom Examples

The zoom feature works with an area selection. By clicking the left mouse button, it's possible to choose an area to apply the zoom.
The right mouse button makes the chart turn back to the last zoomed version.

<Preview name="chart-zoom-feature" />
