# Pagination

<ComponentDescription name="pagination" />

```jsx
<Pagination page={page} onPageChange={(page, type) => {}} total={total} />
```

## Required props

<PropsDocs name="pagination" required />

## Optional props

<PropsDocs name="pagination" />