import { style } from '@vtex/admin-ui-core'

export const container = style({
  display: 'block',
  minHeight: '68px',
  maxHeight: '120px',
  resize: 'none',
  transition: 'snap',
})

export const input = style({
  resize: 'none',
  transition: 'snap',
})
