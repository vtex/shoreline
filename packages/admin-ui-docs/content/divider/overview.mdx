import { Section } from '@site/src/components/Section'
import { IntroSection } from '@site/src/components/IntroSection'

<IntroSection>

## What is this?

Divider containers allow merchants to distinguish between sections in containers with constrained space and to easily identify interactive areas.

</IntroSection>

<br />

:::info
The Divider component uses the `<hr>` HTML tag, which isn’t always the most appropriate way to represent a line. The following recommendations address the application of lines in designs, regardless of whether they are technically a Divider or not.
:::

<br /><br />

<Section hasExplanation>

## Horizontal

By default, the orientation property has a `horizontal`.

```jsx live
<div>
  <div
    className={csx({
      bg: '$info',
      height: '100px',
      width: '100%',
    })}
  />

  <Divider className={csx({ marginY: 6 })} />

  <div
    className={csx({
      bg: '$info',
      height: '100px',
      width: '100%',
    })}
  />
</div>
```

</Section>

<Section hasExplanation>

## Vertical

The value can also be set to `vertical`

```jsx live
<Flex>
    <div
      className={csx({
        bg: '$info',
        height: '100px',
        width: '1/2',
      })}
    />

  <Divider orientation="vertical" className={csx({ marginX: 6 })} />

    <div
      className={csx({
        bg: '$info',
        height: '100px',
        width: '1/2',
      })}
    />
</Flex>
```

</Section>
