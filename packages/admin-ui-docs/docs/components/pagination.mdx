---
title: Pagination
path: /pagination/
hide_table_of_contents: true
---

import { Page, Panel } from '@site/src/components/Page'
import { IntroSection } from '@site/src/components/IntroSection'
import { PageSection } from '@site/src/components/PageSection'
import Admonition from '@theme/Admonition'

export const tabToc = {
  overview: [
    {
      id: 'what-is-this',
      value: 'What is this?',
      level: 2,
    },
    {
      id: 'loading',
      value: 'Loading',
      level: 2,
    },
    {
      id: 'query-string-pagination',
      value: 'Query string pagination',
      level: 2,
    },
    {
      id: 'actions',
      value: 'Actions',
      level: 2,
    },
    {
      id: 'internationalization',
      value: 'Internationalization',
      level: 2,
    },
  ],
  codeReference: [
    {
      id: 'usage',
      value: 'Usage',
      level: 2,
    },
    {
      id: 'props',
      value: 'Props',
      level: 2,
    },
    {
      id: 'state',
      value: 'State',
      level: 2,
    },
    {
      id: 'usepaginationstate-parameters',
      value: 'usePaginationState parameters',
      level: 3,
    },
    {
      id: 'usequerypaginationstate-parameters',
      value: 'useQueryPaginationState parameters',
      level: 3,
    },
    {
      id: 'use-pagination-return',
      value: 'UsePaginationReturn',
      level: 3,
    },
    {
      id: 'pagination-action-type',
      value: 'PaginationActionType',
      level: 3,
    },
  ],
  designReference: [
    {
      id: 'components',
      value: 'Components',
      level: 2,
    },
    {
      id: 'position',
      value: 'Position',
      level: 2,
    },
    {
      id: 'behavior',
      value: 'Behavior',
      level: 2,
    },
  ],
}

<Page toc={tabToc} hasTabs>

<Panel id="overview">

<IntroSection>

## What is this?

Pagination triggers allow merchants to view the size of a list and navigate between pages.

{' '}

<br />
<br />

</IntroSection>

<PageSection
  id={tabToc.overview[1].id}
  title={tabToc.overview[1].value}
>

```jsx live
function Example() {
  const state = usePaginationState({ pageSize: 5, total: 35 })

  return (
    <Flex justify="end">
      <Pagination state={state} loading />
    </Flex>
  )
}
```

</PageSection>

<PageSection
  id={tabToc.overview[2].id}
  title={tabToc.overview[2].value}
>

You can persist the pagination state within the browser's query params using the `useQueryPaginationState` hook, instead of `usePaginationState`. Both hooks are used in the same way. The application should be wrapped by `QueryStateProvider`.

{' '}

<br />
Watch how your url changes. <br /> <br />

```jsx live
function Example() {
  const Content = () => {
    const state = useQueryPaginationState({ pageSize: 5, total: 35 })

    return <Pagination state={state} />
  }

  return (
    <QueryStateProvider>
      <Flex justify="end">
        <Content />
      </Flex>
    </QueryStateProvider>
  )
}
```

</PageSection>

<PageSection
  id={tabToc.overview[3].id}
  title={tabToc.overview[3].value}
>

```jsx live
function Example() {
  const state = usePaginationState({ pageSize: 5, total: 35 })

  const { paginate } = state

  return (
    <Flex justify="space-between">
      <Inline>
        <Button onClick={() => paginate({ type: 'navigate', page: 3 })}>
          Navigate
        </Button>
        <Button onClick={() => paginate({ type: 'prev' })}>Previous</Button>
        <Button onClick={() => paginate({ type: 'next' })}>Next</Button>
        <Button onClick={() => paginate({ type: 'reset' })}>Reset</Button>
        <Button onClick={() => paginate({ type: 'setTotal', total: 100 })}>
          Set total
        </Button>
      </Inline>
      <Pagination state={state} />
    </Flex>
  )
}
```

{' '}

<Admonition type="caution">
  <p>
    Be careful when using the <code>setTotal</code> action, because if you set
    the total to a smaller value it may lead your current page to an invalid
    one.
  </p>
</Admonition>

</PageSection>

<PageSection
  id={tabToc.overview[4].id}
  title={tabToc.overview[4].value}
>

```jsx live
function Example() {
  const state = usePaginationState({ pageSize: 5, total: 35 })
  const I18nProvider = experimental_I18nProvider

  return (
    <I18nProvider locale="pt-BR">
      <Flex justify="end">
        <Pagination state={state} />
      </Flex>
    </I18nProvider>
  )
}
```

</PageSection>

</Panel>

<Panel id="codeReference">

<PageSection
  id={tabToc.codeReference[0].id}
  title={tabToc.codeReference[0].value}
>

```jsx
import { Pagination, usePaginationState } from '@vtex/admin-ui'

function Example() {
  const state = usePaginationState({ pageSize: 5, total: 35 })

  return <Pagination state={state} />
}
```

</PageSection>

<PageSection
  id={tabToc.codeReference[1].id}
  title={tabToc.codeReference[1].value}
>

| Name    | Type                  | Description                         | Required | Default |
| ------- | --------------------- | ----------------------------------- | -------- | ------- |
| state   | `UsePaginationReturn` | Component State                     | ✅       | -       |
| loading | `boolean`             | Whether the table is loading or not | 🚫       | `false` |

</PageSection>

<PageSection
  id={tabToc.codeReference[2].id}
  title={tabToc.codeReference[2].value}
>

The pagination state object is composed of the `currentPage` prop and the `range` prop. The `currentPage` represents the current page as the name says, and the `range` is an array that contains two numbers with the first and the last items positions of the current page.

</PageSection>

<PageSection
  id={tabToc.codeReference[3].id}
  title={tabToc.codeReference[3].value}
  level="3"
>

| Name         | Type                                                                    | Description                                                                        | Required | Default                              |
| ------------ | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | -------- | ------------------------------------ |
| pageSize     | `number`                                                                | Amount of items that will be displayed in a page                                   | ✅       | -                                    |
| total        | `number`                                                                | Total amount of items in a collection                                              | 🚫       | `pageSize value`                     |
| stateReducer | `(state: PaginationState, action: PaginationAction) => PaginationState` | Optional way to provide a function to be used inside usePagination hook useReducer | 🚫       | `usePagination hook default reducer` |
| initialPage  | `number`                                                                | The initial state of pagination                                                    | 🚫       | `1`                                  |
| onNextPage   | `() => void`                                                            | Function called when the next page button is triggered                             | 🚫       | -                                    |
| onPrevPage   | `() => void`                                                            | Function called when the previous page button is triggered                         | 🚫       | -                                    |

</PageSection>

<PageSection
  id={tabToc.codeReference[4].id}
  title={tabToc.codeReference[4].value}
  level="3"
>

| Name         | Type                                                                    | Description                                                                        | Required | Default                              |
| ------------ | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | -------- | ------------------------------------ |
| pageSize     | `number`                                                                | Amount of items that will be displayed in a page                                   | ✅       | -                                    |
| total        | `number`                                                                | Total amount of items in a collection                                              | 🚫       | `pageSize value`                     |
| stateReducer | `(state: PaginationState, action: PaginationAction) => PaginationState` | Optional way to provide a function to be used inside usePagination hook useReducer | 🚫       | `usePagination hook default reducer` |
| onNextPage   | `() => void`                                                            | Function called when the next page button is triggered                             | 🚫       | -                                    |
| onPrevPage   | `() => void`                                                            | Function called when the previous page button is triggered                         | 🚫       | -                                    |

</PageSection>

<PageSection
  id={tabToc.codeReference[5].id}
  title={tabToc.codeReference[5].value}
  level="3"
>

This represents the type of the props returned by both `usePaginationState` and `useQueryPaginationState`. <br /> <br />

| Name          | Type                                   | Description                                                         | Required | Default |
| ------------- | -------------------------------------- | ------------------------------------------------------------------- | -------- | ------- |
| numberOfPages | `number`                               | The total number of pages                                           | ✅       | -       |
| currentPage   | `number`                               | The current page state                                              | ✅       | -       |
| range         | `[number, number]`                     | It represents the positions of the first and last items in the page | ✅       | -       |
| total         | `number`                               | It represents the total number of items                             | ✅       | -       |
| prevDisabled  | `boolean`                              | Whether the navigation to the previous page is disabled or not      | ✅       | -       |
| nextDisabled  | `boolean`                              | Whether the navigation to the next page is disabled or not          | ✅       | -       |
| paginate      | `(args: PaginationActionType) => void` | The initial state of pagination                                     | ✅       | -       |
| onNextPage    | `() => void`                           | Function called when the next page button is triggered              | 🚫       | -       |
| onPrevPage    | `() => void`                           | Function called when the previous page button is triggered          | 🚫       | -       |

</PageSection>

<PageSection
  id={tabToc.codeReference[6].id}
  title={tabToc.codeReference[6].value}
  level="3"
>

```ts
export type PaginationActionType =
  | {
      type: 'next' | 'prev' | 'reset'
    }
  | {
      type: 'setTotal'
      total: number
    }
  | {
      type: 'navigate'
      page: number
    }
```

</PageSection>

</Panel>

<Panel id="designReference">

<PageSection
  id={tabToc.designReference[0].id}
  title={tabToc.designReference[0].value}
>

<details>
  <summary>
    <h4>When to use the Pagination component or infinite scroll?</h4>
  </summary>
  <div>
    Use Pagination when a list can have more than 25 results. Infinite scroll is
    not recommended in any scenario.
  </div>
</details>

</PageSection>

<PageSection
  id={tabToc.designReference[1].id}
  title={tabToc.designReference[1].value}
>

<details>
  <summary>
    <h4>Where should Pagination be positioned in a container?</h4>
  </summary>
  <div>
    Considering left-to-right interfaces, follow these recommendations:
    <ul>
      <li>Pagination should always be in the top right of a container.</li>
      <li>
        If the list has a vertical scroll, the Pagination should also be in the
        bottom right.
      </li>
      <li>
        Use the{' '}
        <a href="https://admin-ui.vercel.app/components/data-view">DataView</a>{' '}
        component to correctly position the Pagination.
      </li>
    </ul>
  </div>
  <br />
  <div>
    <strong>What to avoid:</strong>
    <ul>
      <li>Don’t position the Pagination in the header of a container.</li>
    </ul>
  </div>
</details>

</PageSection>

<PageSection
  id={tabToc.designReference[2].id}
  title={tabToc.designReference[2].value}
>

<details>
  <summary>
    <h4>How many items should be shown per page of a list?</h4>
  </summary>
  <div>
    <ul>
      <li>The standard is to show 25 items per page of a list.</li>
      <li>
        When the list is inside a container where vertical scroll should be
        avoided, such as a Modal or a Card, limit the amount of items
        accordingly.
      </li>
    </ul>
  </div>
  <br />
  <div>
    <strong>What to avoid:</strong>
    <ul>
      <li>
        Don’t allow merchants to customize the number of results per page.
      </li>
    </ul>
  </div>
</details>

<details>
  <summary>
    <h4>When to show a loading Pagination?</h4>
  </summary>
  <div>
    When the list is loading, since the number of results is not loaded and the
    user can’t navigate between pages yet.
  </div>
</details>

<details>
  <summary>
    <h4>When should the number of results be updated?</h4>
  </summary>
  <div>
    <ul>
      <li>
        When the merchant edits the filter values or the search query,
        the total number of results should inform the current state of the list.
      </li>
      <li>
        Only load new items when the merchant explicitly triggers an action to
        update the results, such as the <em>Refresh</em> button in the web
        browser.
      </li>
      <li>
        When the user has just created an item and returns to the listing page,
        they should go to the first page with updated results.
      </li>
      <li>
        When the user has just edited an item and returns to the listing page,
        they should see a list with the same items as before, but with the
        edited item updated.
      </li>
    </ul>
  </div>
  <br />
  <div>
    <strong>What to avoid:</strong>
    <ul>
      <li>
        Don’t load new items while the merchant is navigating between pages.
      </li>
    </ul>
  </div>
</details>

<details>
  <summary>
    <h4>When should the current state of a list be stored in the URL?</h4>
  </summary>
  <div>
    The complete search query, which includes the filters, the current page and
    the search terms, should be reflected in the URL. This allows the merchant
    to easily return to the customized view and easily share it.
  </div>
  <br />
  <div>
    <strong>What to avoid:</strong>
    <ul>
      <li>Don’t use local storage or cookies to store the query.</li>
    </ul>
  </div>
</details>

</PageSection>

</Panel>

</Page>
