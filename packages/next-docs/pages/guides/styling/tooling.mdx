# Tooling

## CSS

The Shoreline team uses [Sass](https://sass-lang.com/) under the hood to apply style to its components. This doesn't means that you have to use `Sass` to customize your application. You can use any CSS preprocessor or even plain CSS. To do that you can use the [Shoreline CSS theme] available with every token.

## Stylelint

## VS Code plugins
