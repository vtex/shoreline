import { Tab, Tabs } from 'nextra-theme-docs'
import { Callout } from 'nextra/components'

<Callout type="info" emoji="💡">
  This package is inteded to be used by Shoreline contributors and library
  authors.
</Callout>

# Introduction

The `@shoreline/csx` package

## Install

<Tabs items={['yarn', 'npm', 'pnpm']}>
  <Tab>

    ```sh
    yarn add @shoreline/csx
    ```

  </Tab>
  <Tab>
  
    ```sh
    npm install @shoreline/csx
    ```
  
  </Tab>
  <Tab>
  
    ```sh
    pnpm add @shoreline/csx
    ```
  
  </Tab>
</Tabs>

## Quick start

```ts
import { csx } from '@shoreline/csx'

const styles = csx({
  backgroundColor: 'black',
})
```

## Make it reusable

```ts
import { csx as _csx, CSXObject } from '@shoreline/csx'
import { style } from '@vanilla-extract/css'

export function csx(csxObject: CSXObject = {}): string {
  const cssObject = _csx(csxObject)
  return style(cssObject)
}
```
