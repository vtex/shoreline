# Commands

Available commands of the CLI.

## theme

Generates a theme in CSS format and a typescript file with all the token types, given the configuration file.

```sh
shorelinecss theme
```

## typescript-theme

Generates a theme in typescript format given the configuration file.

```sh
shorelinecss typescript-theme
```
