const colors = {
  gray05: '#F4F4F4',
  gray10: '#E6E6E6',
}

const bg = {
  secondary: colors.gray05,
  skeleton: `linear-gradient(90deg, transparent, ${colors.gray10}, transparent)`,
}

const radii = {
  default: '4px',
}

export const tokens = {
  bg,
  radii,
}
