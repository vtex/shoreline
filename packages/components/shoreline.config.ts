import { defineConfig } from '@vtex/shoreline-theme'
import { presetAdmin as preset } from '@vtex/shoreline-preset-admin'
// import { preset } from '@vtex/shoreline-preset-faststore'

export default defineConfig({
  preset,
})
