{
    "index": {
        "title": "All Components",
        "theme": {
        "layout": "full"
        }
    },
    {{#if components}}
    {{#each components}}
    "{{{key}}}": "{{{value}}}" {{#unless @last}},{{/unless}}
    {{/each}}
    {{/if}}
}
