import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'
import { Divider } from './index'

<Meta title="design-system/components/Divider" />

# Admin UI Divider

It renders an `hr` element and grants accessibility described on the [WAI-ARIA Separator Role](https://www.w3.org/TR/wai-aria-1.1/#separator)

## Usage

```jsx
import { Divider, DividerProps } from '@vtex/admin-ui'

function UseCase() {
  return <Divider orientation="horizontal" />
}
```

### Horizontal

<Canvas>
  <Story id="beta-divider--horizontal" />
</Canvas>

### Vertical

<Canvas>
  <Story id="beta-divider--vertical" />
</Canvas>

## Props

<Props of={Divider} />

## Customization

You can use all margin's `SpaceTokens` to customize your divider, and also you can use the `sx` prop to add any style to your component.

> 💡 You can check the [Theme Documentation](https://admin-ui.vercel.app/?path=/story/design-system-theme--page) for detailed info.

### Example

- Increasing vertical margin using `SpaceTokens`

```jsx
import { Divider, DividerProps } from '@vtex/admin-ui'

function UseCase() {
  return <Divider my="13" orientation="horizontal" />
}
```

- Increasing vertical margin using `sx`

```jsx
import { Divider, DividerProps } from '@vtex/admin-ui'

function UseCase() {
  return <Divider sx={{ marginY: '13' }} orientation="horizontal" />
}
```
