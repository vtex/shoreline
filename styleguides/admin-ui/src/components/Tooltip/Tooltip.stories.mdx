import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'

import { Tooltip } from './index'

<Meta title="design-system/components/Tooltip" />

# Beta Tooltip

Tooltip follows the WAI-ARIA Tooltip Pattern. It's a popup that displays information related to an element when the element receives keyboard focus or the mouse hovers over it.

- Tooltip
- version: beta.1

## Behavior

<Canvas>
  <Story id="beta-tooltip--string-label" />
</Canvas>

## Installation

```bash
yarn add @vtex/admin-ui
```

```tsx
import { Tooltip } from '@vtex/admin-ui'
```

## String label

<Canvas>
  <Story id="beta-tooltip--string-label" />
</Canvas>

## ReactNode label

<Canvas>
  <Story id="beta-tooltip--react-node-label" />
</Canvas>

## Custom Component

<Canvas>
  <Story id="beta-tooltip--custom-component" />
</Canvas>

## Props

<Props of={Tooltip} />

## Customization

You can customize the tooltip using the `sx` prop.

## Limitations

When using the Tooltip with a custom component, you must provide a `ref` for Popup to work. You can easily do this using the `React.forwardRef`. You can check the example below to see how it works.

##### Example

<Canvas>
  <Story id="beta-tooltip--custom-component" />
</Canvas>
