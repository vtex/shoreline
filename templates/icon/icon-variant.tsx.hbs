import React, { forwardRef, ComponentPropsWithoutRef } from 'react'

export const Icon{{pascalCase name}}{{titleCase variant}} = forwardRef<SVGSVGElement, ComponentPropsWithoutRef<'svg'>>(function Icon{{pascalCase name}}{{titleCase variant}}(
  props, 
  ref
) {
  return (
    <svg
      data-sl-icon
      width="16"
      height="16"
      viewBox="0 0 16 16"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      ref={ref}
      {...props}
    >
      ...
    </svg>
  )
})
