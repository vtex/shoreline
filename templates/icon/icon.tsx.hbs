import React, { forwardRef, ComponentPropsWithoutRef } from 'react'

export const Icon{{pascalCase name}} = forwardRef<SVGSVGElement, ComponentPropsWithoutRef<'svg'>>(function Icon{{pascalCase name}}(
  props, 
  ref
) {
  return (
    <svg
      width="20"
      height="20"
      viewBox="0 0 20 20"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      ref={ref}
      {...props}
    >
      ...
    </svg>
  )
})
